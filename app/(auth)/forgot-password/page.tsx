// Stub — full implementation lands in Phase A.3 Chunk 2 (forgot/reset
// password flow + email template). The link from sign-in already points
// here so typed routes resolves at compile time.
export const metadata = { title: "שכחתי סיסמה · AccounTech" };

export default function ForgotPasswordPage() {
  return (
    <section className="glass-strong rounded-2xl p-8 text-center">
      <h1 className="text-2xl font-semibold text-slate-100">איפוס סיסמה</h1>
      <p className="mt-3 text-sm text-slate-400">בקרוב — Phase A.3 Chunk 2.</p>
    </section>
  );
}
